[project]
name = "kestrel"
version = "0.1.0"
description = "Open-source circuit generator framework"
requires-python = ">=3.10"

[project.scripts]
kestrel = "kestrel.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
